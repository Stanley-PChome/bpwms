<?xml version="1.0" encoding="utf-8"?>
<uiLib:Wms3plUserControl x:Class="Wms3pl.WpfClient.P19.Views.P1902030000" xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" xmlns:uiLib="clr-namespace:Wms3pl.WpfClient.UILib;assembly=Wms3pl.WpfClient.UILib" xmlns:controls="clr-namespace:Wms3pl.WpfClient.UILib.Controls;assembly=Wms3pl.WpfClient.UILib" xmlns:common="clr-namespace:Wms3pl.WpfClient.Common;assembly=Wms3pl.WpfClient.Common" xmlns:viewModel="clr-namespace:Wms3pl.WpfClient.P19.ViewModel" xmlns:Properties="clr-namespace:Wms3pl.WpfClient.P19.Properties" xmlns:res="clr-namespace:Wms3pl.WpfClient.Resources;assembly=Wms3pl.WpfClient.Resources" x:Name="Window" Margin="0,0,0,0" HorizontalAlignment="Stretch" FontSize="18" MinWidth="800" xmlns:converter="clr-namespace:Wms3pl.WpfClient.Common.Converters;assembly=Wms3pl.WpfClient.Common" controls:PermissionService.FunctionId="P1902030000" controls:PermissionService.FunctionName="原因編碼檔" FormException="{Binding Exception}">
  <uiLib:Wms3plUserControl.DataContext>
    <viewModel:P1902030000_ViewModel x:Name="Vm" />
  </uiLib:Wms3plUserControl.DataContext>
  <uiLib:Wms3plUserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Wms3pl.WpfClient.UILib;component/Generic.xaml" />
      </ResourceDictionary.MergedDictionaries>
      <common:NotConverter x:Key="NotConverter" />
      <converter:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter" />
      <viewModel:P1902030000_ViewModel x:Key="Vm" />
      <converter:CheckStateConverter x:Key="CheckStateConverter" />
    </ResourceDictionary>
  </uiLib:Wms3plUserControl.Resources>
  <telerik:RadBusyIndicator x:Name="GridBusyIndicator" BusyContent="{x:Static res:Resources.Loading}" IsIndeterminate="False" IsBusy="{Binding IsBusy}">
    <uiLib:BasicMaster x:Name="Master">
      <uiLib:BasicMaster.ToolBarArea>
        <WrapPanel>
          <Button Style="{StaticResource SearchButton}" controls:PermissionService.FunctionId="BP1902030001" Command="{Binding SearchCommand}" />
          <Button Style="{StaticResource AddButton}" controls:PermissionService.FunctionId="BP1902030002" Command="{Binding AddCommand}" />
          <Button Style="{StaticResource EditButton}" controls:PermissionService.FunctionId="BP1902030003" Command="{Binding EditCommand}" />
          <Button Style="{StaticResource CancelButton}" controls:PermissionService.FunctionId="BP1902030004" Command="{Binding CancelCommand}" />
          <Button Style="{StaticResource DeleteButton}" controls:PermissionService.FunctionId="BP1902030005" Command="{Binding DeleteCommand}" />
          <Button Style="{StaticResource SaveButton}" controls:PermissionService.FunctionId="BP1902030006" Command="{Binding SaveCommand}" IsEnabled="{Binding Path=(Validation.HasError), ElementName=LayoutRoot, Converter={StaticResource NotConverter}}" />
        </WrapPanel>
      </uiLib:BasicMaster.ToolBarArea>
      <uiLib:BasicMaster.MainContent>
        <TabControl x:Name="LayoutRoot">
          <TabControl.BindingGroup>
            <BindingGroup Name="errorGroup" />
          </TabControl.BindingGroup>
          <TabItem Header="{x:Static res:Resources.Main}" Width="Auto">
            <DockPanel>
              <!-- 由此處開始加入畫面項目 -->
              <GroupBox Header="{x:Static res:Resources.Query}" DockPanel.Dock="Top" Margin="0,0,0,10" Visibility="{Binding UserOperateMode ,Converter={StaticResource EnumToVisibilityConverter},ConverterParameter={x:Static uiLib:OperateMode.Query}}">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                  <Label Content="{x:Static Properties:Resources.cbUctId}" HorizontalAlignment="Left" Margin="3" VerticalAlignment="Center" />
                  <ComboBox SelectedValue="{Binding cbUctId,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" x:Name="cbUctId" ItemsSource="{Binding F1950S}" Margin="3,4,0,0" VerticalAlignment="Top" MinWidth="140" SelectedValuePath="UCT_ID" DisplayMemberPath="TYPE_DESC"></ComboBox>
                  <Label Content="{x:Static Properties:Resources.txtUCC_CODE}" HorizontalAlignment="Left" Margin="3" VerticalAlignment="Center" />
                  <TextBox Text="{Binding txtUCC_CODE,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" MinHeight="23" HorizontalAlignment="Left" Margin="3,4,0,0" Name="txtUCC_CODE" VerticalAlignment="Top" MinWidth="140" />
                  <Label Content="{x:Static Properties:Resources.txtCAUSE}" HorizontalAlignment="Left" Margin="3" VerticalAlignment="Center" Foreground="Blue" />
                  <TextBox Text="{Binding  txtCAUSE,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" MinHeight="23" HorizontalAlignment="Left" Margin="3,4,0,0" Name="txtCAUSE" VerticalAlignment="Top" MinWidth="140" />
                </StackPanel>
              </GroupBox>
              <controls:ValidationDataGrid SelectionMode="Single" ItemsSource="{Binding DgList,UpdateSourceTrigger=PropertyChanged}" Name="dgList" SelectedItem="{Binding SelectedData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DgStyle}" IsReadOnly="False" CellStyle="{StaticResource UcSingleRowEdit}" RowHeaderStyle="{StaticResource UcSingleRowHeaderEdit}">
                <DataGrid.Columns>
                  <DataGridComboBoxColumn Header="{x:Static Properties:Resources.cbUctId}" IsReadOnly="True" ItemsSource="{Binding F1950S, Source={StaticResource Vm}}" SelectedValuePath="UCT_ID" DisplayMemberPath="TYPE_DESC" SelectedValueBinding="{Binding UCT_ID}">
                    <DataGridComboBoxColumn.CellStyle>
                      <Style TargetType="DataGridCell" BasedOn="{StaticResource UcSingleRowAddEditFocus}" />
                    </DataGridComboBoxColumn.CellStyle>
                    <DataGridComboBoxColumn.EditingElementStyle>
                      <Style TargetType="ComboBox" BasedOn="{StaticResource DataGridEditingElement}">
                        <EventSetter Event="GotFocus" Handler="CellElement_GotFocus" />
                      </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>
                  </DataGridComboBoxColumn>
                  <DataGridTextColumn Header="{x:Static Properties:Resources.txtUCC_CODE}" IsReadOnly="True" Binding="{Binding UCC_CODE}">
                    <DataGridTextColumn.EditingElementStyle>
                      <Style TargetType="TextBox">
                        <Setter Property="MaxLength" Value="4"></Setter>
                      </Style>
                    </DataGridTextColumn.EditingElementStyle>
                  </DataGridTextColumn>
                  <DataGridTextColumn Header="{x:Static Properties:Resources.txtCAUSE}" IsReadOnly="True" Binding="{Binding CAUSE}">
                    <DataGridTextColumn.EditingElementStyle>
                      <Style TargetType="TextBox">
                        <Setter Property="MaxLength" Value="40"></Setter>
                      </Style>
                    </DataGridTextColumn.EditingElementStyle>
                  </DataGridTextColumn>
                </DataGrid.Columns>
              </controls:ValidationDataGrid>
            </DockPanel>
          </TabItem>
        </TabControl>
      </uiLib:BasicMaster.MainContent>
    </uiLib:BasicMaster>
  </telerik:RadBusyIndicator>
</uiLib:Wms3plUserControl>