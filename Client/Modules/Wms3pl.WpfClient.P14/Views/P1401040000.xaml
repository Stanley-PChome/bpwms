<?xml version="1.0" encoding="utf-8"?>
<uiLib:Wms3plUserControl x:Class="Wms3pl.WpfClient.P14.Views.P1401040000" xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" xmlns:uiLib="clr-namespace:Wms3pl.WpfClient.UILib;assembly=Wms3pl.WpfClient.UILib" xmlns:controls="clr-namespace:Wms3pl.WpfClient.UILib.Controls;assembly=Wms3pl.WpfClient.UILib" xmlns:converters="clr-namespace:Wms3pl.WpfClient.Common.Converters;assembly=Wms3pl.WpfClient.Common" xmlns:common="clr-namespace:Wms3pl.WpfClient.Common;assembly=Wms3pl.WpfClient.Common" xmlns:viewModel="clr-namespace:Wms3pl.WpfClient.P14.ViewModel" xmlns:Properties="clr-namespace:Wms3pl.WpfClient.P14.Properties" xmlns:res="clr-namespace:Wms3pl.WpfClient.Resources;assembly=Wms3pl.WpfClient.Resources" xmlns:views="clr-namespace:Wms3pl.WpfClient.UcLib.Views;assembly=Wms3pl.WpfClient.UcLib" x:Name="Window" Margin="0,0,0,0" HorizontalAlignment="Stretch" FontSize="18" MinWidth="800" controls:PermissionService.FunctionId="P1401040000" controls:PermissionService.FunctionName="盤盈損查詢與列印" FormException="{Binding Exception}">
  <uiLib:Wms3plUserControl.DataContext>
    <viewModel:P1401040000_ViewModel x:Name="Vm" />
  </uiLib:Wms3plUserControl.DataContext>
  <uiLib:Wms3plUserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Wms3pl.WpfClient.UILib;component/Generic.xaml" />
      </ResourceDictionary.MergedDictionaries>
      <common:NotConverter x:Key="NotConverter" />
      <converters:DataGridRowNumberConverter x:Key="DataGridRowNumberConverter" />
      <converters:DateTimeToStringConverter x:Key="DateTimeToStringConverter" />
      <converters:RadioBoolToStringConverter x:Key="RadioBoolToStringConverter" />
      <converters:StringToBoolConverter x:Key="StringToBoolConverter" />
       <converters:StringToNotBoolConverter x:Key="StringToNotBoolConverter" />
      <converters:StringToLocCodeConverter x:Key="StringToLocCodeConverter" />
      <FrameworkElement x:Key="ProxyElement" DataContext="{Binding}" />      
        <Style TargetType="TextBox" BasedOn="{StaticResource TxtStyle}" x:Key="LargeTxtStyle">
        <Setter Property="Width" Value="180" />
      </Style>
      <Style TargetType="ComboBox" BasedOn="{StaticResource ComboStyle}" x:Key="LargeComboStyle">
        <Setter Property="Width" Value="180" />
      </Style>
    </ResourceDictionary>
  </uiLib:Wms3plUserControl.Resources>
  <telerik:RadBusyIndicator x:Name="GridBusyIndicator" BusyContent="{x:Static res:Resources.Loading}" IsIndeterminate="False" IsBusy="{Binding IsBusy}">
    <uiLib:BasicMaster x:Name="Master">
      <uiLib:BasicMaster.ToolBarArea>
        <WrapPanel>
          <Button Style="{StaticResource SearchButton}" controls:PermissionService.FunctionId="BP1401040001" Command="{Binding SearchCommand}" />
          <Button Style="{StaticResource PreviewButton}" controls:PermissionService.FunctionId="BP1401040002" Command="{Binding PrintCommand}" CommandParameter="{x:Static uiLib:PrintType.Preview}" />
          <Button Style="{StaticResource PrintButton}" controls:PermissionService.FunctionId="BP1401040003" Command="{Binding PrintCommand}" CommandParameter="{x:Static uiLib:PrintType.ToPrinter}" />
        </WrapPanel>
      </uiLib:BasicMaster.ToolBarArea>
      <uiLib:BasicMaster.MainContent>
        <TabControl x:Name="LayoutRoot">
          <TabControl.BindingGroup>
            <BindingGroup Name="errorGroup" />
          </TabControl.BindingGroup>
          <TabItem Header="{x:Static res:Resources.Main}" Width="Auto">
            <ScrollViewer>
              <DockPanel>
                <DockPanel DockPanel.Dock="Top">
                  <GroupBox Header="{x:Static res:Resources.QueryCondition}">
                    <Expander IsExpanded="True">
                      <WrapPanel DockPanel.Dock="TOP" Margin="5">
                        <Grid>
                          <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                          </Grid.RowDefinitions>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                          </Grid.ColumnDefinitions>
                          <Label Grid.Row="0" Grid.Column="0" Content="{x:Static res:Resources.Dc}" HorizontalAlignment="Right" Style="{StaticResource LbStyle}" />
                          <ComboBox Style="{StaticResource ComboStyle}" Grid.Row="0" Grid.Column="1" ItemsSource="{Binding DcList,UpdateSourceTrigger=PropertyChanged}" SelectedValue="{Binding SelectedDcCode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Value" DisplayMemberPath="Name" />
                          <Label Grid.Row="0" Grid.Column="2" Content="{x:Static Properties:Resources.PostingDateBegin}" HorizontalAlignment="Right" Style="{StaticResource LbStyle}" />
                          <DatePicker Grid.Row="0" Grid.Column="3" SelectedDate="{Binding Path=PostingDateBegin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DateStyle}" />
                          <Label Content=" ~ " Grid.Row="0" Grid.Column="4" Style="{StaticResource LbStyle}" MinWidth="20" HorizontalAlignment="Center" />
                          <DatePicker Grid.Row="0" Grid.Column="5" SelectedDate="{Binding Path=PostingDateEnd, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DateStyle}" />
                        </Grid>
                      </WrapPanel>
                    </Expander>
                  </GroupBox>
                </DockPanel>
                <DockPanel DockPanel.Dock="Top">
                  <GroupBox Header="{x:Static res:Resources.OueryResult}">
                    <Expander IsExpanded="True">
                      <DataGrid Style="{StaticResource DgStyle}" Height="150" ItemsSource="{Binding Records,UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedRecord,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" SelectionMode="Single">
                        <DataGrid.Columns>
                          <DataGridTextColumn Header="{x:Static res:Resources.ItemNumber}">
                            <DataGridTextColumn.Binding>
                              <MultiBinding Converter="{StaticResource DataGridRowNumberConverter}">
                                <Binding />
                                <Binding RelativeSource="{RelativeSource FindAncestor,AncestorType=DataGrid}" />
                              </MultiBinding>
                            </DataGridTextColumn.Binding>
                          </DataGridTextColumn>
                          <DataGridTextColumn Header="{x:Static Properties:Resources.QueryInventorySDate}" Binding="{Binding INVENTORY_DATE,Converter={StaticResource DateTimeToStringConverter}, ConverterParameter=yyyy/MM/dd}" />
                          <DataGridTextColumn Header="{x:Static Properties:Resources.QueryInventoryNo}" Binding="{Binding INVENTORY_NO}" />
                          <DataGridTextColumn Header="{x:Static Properties:Resources.PostingDateBegin}" Binding="{Binding POSTING_DATE,Converter={StaticResource DateTimeToStringConverter}, ConverterParameter=yyyy/MM/dd HH:mm}" />
                        </DataGrid.Columns>
                      </DataGrid>
                    </Expander>
                  </GroupBox>
                </DockPanel>
                <Grid DockPanel.Dock="Top">
                  <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                  </Grid.RowDefinitions>
                  <Grid.ColumnDefinitions>
                   <ColumnDefinition Width="7*"/>
                   <ColumnDefinition Width="2*" />
                  </Grid.ColumnDefinitions>
                  <GroupBox Header="{x:Static Properties:Resources.FilterCondition}" Grid.Column="0" Grid.Row="0">
                    <Expander IsExpanded="False" Name="ExpFilter">
                      <Grid Margin="5">
                        <Grid.RowDefinitions>
                          <RowDefinition Height="auto" />
                          <RowDefinition Height="auto" />
                          <RowDefinition Height="auto" />
                          <RowDefinition Height="auto" />
                          <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="7*" />
                          </Grid.ColumnDefinitions>
                        <RadioButton GroupName="RbFilter" Content="{x:Static Properties:Resources.WarehouseId}" Grid.Column="0" Grid.Row="0" FontWeight="Bold" IsChecked="{Binding FilterType,Mode = TwoWay , UpdateSourceTrigger=PropertyChanged,Converter= {StaticResource RadioBoolToStringConverter},ConverterParameter=0}" />
                         <ComboBox Style="{StaticResource ComboStyle}" Grid.Row="0" Grid.Column="1" ItemsSource="{Binding WarehouseList,UpdateSourceTrigger=PropertyChanged}" SelectedValue="{Binding SelectedWarehouseId,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Value" DisplayMemberPath="Name" IsEnabled="{Binding FilterType,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource StringToNotBoolConverter}}"/>
                        <RadioButton GroupName="RbFilter" Content="{x:Static Properties:Resources.IsItemCode}" Grid.Column="0" Grid.Row="1" FontWeight="Bold" IsChecked="{Binding FilterType,Mode = TwoWay , UpdateSourceTrigger=PropertyChanged,Converter= {StaticResource RadioBoolToStringConverter},ConverterParameter=1}" />
                        
                        <WrapPanel Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2"  IsEnabled="{Binding FilterType,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource StringToBoolConverter}}">
                         <views:UcSearchProduct ItemCode="{Binding ITEM_CODE,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" ItemName="{Binding ITEM_NAME,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" ItemSize="{Binding ITEM_SIZE,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" ItemSpec="{Binding ITEM_SPEC,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" ItemColor="{Binding ITEM_COLOR,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" SerialNo="{Binding SERIAL_NO,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" HasItem="{Binding HasItem, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                        </WrapPanel>
                          <WrapPanel Grid.Column="0" Grid.Row="3" Margin="5" Grid.ColumnSpan="2">
                          <Button Content="{x:Static Properties:Resources.ProductId}" controls:PermissionService.FunctionId="BP1401040005" Command="{Binding AddItemCommand}" />
                          <Button Content="{x:Static res:Resources.Delete}" controls:PermissionService.FunctionId="BP1401040006" Command="{Binding DeleteItemCommand}" Margin="10,0" />
                        </WrapPanel>
                         <WrapPanel Grid.Column="0" Grid.Row="4" Grid.ColumnSpan="2">
                         <DataGrid Style="{StaticResource DgStyle}" Width="900" ScrollViewer.HorizontalScrollBarVisibility="Auto" ItemsSource="{Binding FilterProductList}" IsReadOnly="False" SelectedItem="{Binding SelectedEditDetailItem,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" CellStyle="{StaticResource SingleClickEdit}">
                            <DataGrid.Columns>
                              <DataGridCheckBoxColumn Binding="{Binding IsSelected,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                                <DataGridCheckBoxColumn.HeaderTemplate>
                                  <DataTemplate>
                                    <CheckBox Style="{StaticResource CheckStyle}" Content="{x:Static res:Resources.SelectAll}" IsChecked="{Binding DataContext.IsCheckAll, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl},Mode = TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                                  </DataTemplate>
                                </DataGridCheckBoxColumn.HeaderTemplate>
                                <DataGridCheckBoxColumn.ElementStyle>
                                  <Style TargetType="CheckBox">
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="HorizontalAlignment" Value="Center" />
                                  </Style>
                                </DataGridCheckBoxColumn.ElementStyle>
                              </DataGridCheckBoxColumn>
                              <DataGridTextColumn Header="{x:Static res:Resources.ItemNumber}" IsReadOnly="True">
                                <DataGridTextColumn.Binding>
                                  <MultiBinding Converter="{StaticResource DataGridRowNumberConverter}">
                                    <Binding />
                                    <Binding RelativeSource="{RelativeSource FindAncestor,AncestorType=DataGrid}" />
                                  </MultiBinding>
                                </DataGridTextColumn.Binding>
                              </DataGridTextColumn>
                              <DataGridTextColumn Header="{x:Static Properties:Resources.ITEM_CODE}" Binding="{Binding Item.ITEM_CODE}" IsReadOnly="True" />
                              <DataGridTextColumn Header="{x:Static Properties:Resources.ITEM_NAME}" Binding="{Binding Item.ITEM_NAME}" IsReadOnly="True" />
                              <DataGridTextColumn Header="{x:Static Properties:Resources.ITEM_SIZE}" Binding="{Binding Item.ITEM_SIZE}" IsReadOnly="True" />
                              <DataGridTextColumn Header="{x:Static Properties:Resources.ITEM_SPEC}" Binding="{Binding Item.ITEM_SPEC}" IsReadOnly="True" />
                              <DataGridTextColumn Header="{x:Static Properties:Resources.ITEM_COLOR}" Binding="{Binding Item.ITEM_COLOR}" IsReadOnly="True" />
                            </DataGrid.Columns>
                          </DataGrid>
                        </WrapPanel>
                      </Grid>
                    </Expander>
                  </GroupBox>
                  <WrapPanel Grid.Column="1" Grid.Row="0" Margin="5,15">
                    <Label Content="{x:Static Properties:Resources.SortCondition}" />
                    <CheckBox VerticalAlignment="Center" IsChecked="{Binding SortByCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Label Content="{x:Static Properties:Resources.SortByCount}" />
                  </WrapPanel>
                </Grid>
                <WrapPanel DockPanel.Dock="Top" Margin="8">
                  <Button controls:PermissionService.FunctionId="BP1401040004" Content="{x:Static Properties:Resources.SearchDetail}" Command="{Binding SearchDetailCommand}" />
                </WrapPanel>
                <DockPanel DockPanel.Dock="Top">
                  <GroupBox Header="{x:Static Properties:Resources.DetailRecords}">
                    <Expander IsExpanded="False" Name="ExpDetail">
                       <DataGrid Style="{StaticResource DgStyle}"   AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ItemsSource="{Binding DetailRecords,UpdateSourceTrigger=PropertyChanged}" SelectionMode="Single" DockPanel.Dock="Top" Height="400" >
                           <DataGrid.Columns>
                               <DataGridTextColumn Header="{x:Static res:Resources.ItemNumber}" IsReadOnly="True">
                                   <DataGridTextColumn.Binding>
                                       <MultiBinding Converter="{StaticResource DataGridRowNumberConverter}">
                                           <Binding />
                                           <Binding RelativeSource="{RelativeSource FindAncestor,AncestorType=DataGrid}" />
                                       </MultiBinding>
                                   </DataGridTextColumn.Binding>
                               </DataGridTextColumn>
                               <DataGridTextColumn Header="{x:Static Properties:Resources.ITEM_CODE}" Binding="{Binding ITEM_CODE}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.ITEM_NAME}" Binding="{Binding ITEM_NAME}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.ITEM_SIZE}" Binding="{Binding ITEM_SIZE}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.ITEM_SPEC}" Binding="{Binding ITEM_SPEC}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.ITEM_COLOR}" Binding="{Binding ITEM_COLOR}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.MAKE_NO}" Binding="{Binding MAKE_NO}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.VALID_DATE}" Binding="{Binding VALID_DATE, Converter={StaticResource DateTimeToStringConverter}, ConverterParameter=yyyy/MM/dd}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.EnterDate}" Binding="{Binding ENTER_DATE, Converter={StaticResource DateTimeToStringConverter}, ConverterParameter=yyyy/MM/dd}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.QueryDetialWareHouseId}" Binding="{Binding WAREHOUSE_NAME}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.LocCode}" Binding="{Binding LOC_CODE, Converter={StaticResource StringToLocCodeConverter}}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.QTY}" Binding="{Binding QTY}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.PROFIT_QTY}" Binding="{Binding PROFIT_QTY}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.LOSS_QTY}" Binding="{Binding LOSS_QTY}" />
                               <DataGridCheckBoxColumn Header="{x:Static Properties:Resources.FLUSHBACK}" Binding="{Binding FLUSHBACK, Converter={StaticResource StringToBoolConverter}}" IsReadOnly="True"></DataGridCheckBoxColumn>
                               <DataGridTextColumn Header="{x:Static Properties:Resources.PALLET_CTRL_NO}" Binding="{Binding PALLET_CTRL_NO}" />
                               <DataGridTextColumn Header="{x:Static Properties:Resources.BOX_CTRL_NO}" Binding="{Binding BOX_CTRL_NO}" />
                           </DataGrid.Columns>
                       </DataGrid>
                      </Expander>
                  </GroupBox>
                </DockPanel>
                               
                            </DockPanel>
            </ScrollViewer>
          </TabItem>
        </TabControl>
      </uiLib:BasicMaster.MainContent>
    </uiLib:BasicMaster>
  </telerik:RadBusyIndicator>
</uiLib:Wms3plUserControl>