<?xml version="1.0" encoding="utf-8"?>
<uiLib:Wms3plUserControl x:Class="Wms3pl.WpfClient.P05.Views.P0502010000" xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" xmlns:uiLib="clr-namespace:Wms3pl.WpfClient.UILib;assembly=Wms3pl.WpfClient.UILib" xmlns:controls="clr-namespace:Wms3pl.WpfClient.UILib.Controls;assembly=Wms3pl.WpfClient.UILib" xmlns:common="clr-namespace:Wms3pl.WpfClient.Common;assembly=Wms3pl.WpfClient.Common" xmlns:converters="clr-namespace:Wms3pl.WpfClient.Common.Converters;assembly=Wms3pl.WpfClient.Common" xmlns:Properties="clr-namespace:Wms3pl.WpfClient.P05.Properties" xmlns:res="clr-namespace:Wms3pl.WpfClient.Resources;assembly=Wms3pl.WpfClient.Resources" xmlns:viewModel="clr-namespace:Wms3pl.WpfClient.P05.ViewModel" x:Name="Window" Margin="0,0,0,0" HorizontalAlignment="Stretch" FontSize="18" MinWidth="800" controls:PermissionService.FunctionId="P0502010000" controls:PermissionService.FunctionName="出貨碼頭分配" FormException="{Binding Exception}">
  <uiLib:Wms3plUserControl.DataContext>
    <viewModel:P0502010000_ViewModel x:Name="Vm" />
  </uiLib:Wms3plUserControl.DataContext>
  <uiLib:Wms3plUserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Wms3pl.WpfClient.UILib;component/Generic.xaml" />
      </ResourceDictionary.MergedDictionaries>
      <common:NotConverter x:Key="NotConverter" />
      <converters:DateTimeToStringConverter x:Key="DateTimeToStringConverter" />
      <converters:NameValuePairMultiConverter x:Key="NameValuePairMultiConverter" />
    </ResourceDictionary>
  </uiLib:Wms3plUserControl.Resources>
  <telerik:RadBusyIndicator x:Name="GridBusyIndicator" BusyContent="{x:Static res:Resources.Loading}" IsIndeterminate="False">
    <uiLib:BasicMaster x:Name="Master">
      <uiLib:BasicMaster.ToolBarArea>
        <WrapPanel>
          <Button Style="{StaticResource SearchButton}" controls:PermissionService.FunctionId="BP0502010001" Command="{Binding SearchCommand}" />
          <Button Style="{StaticResource SaveButton}" controls:PermissionService.FunctionId="BP0502010006" Command="{Binding SaveCommand}" IsEnabled="{Binding Path=(Validation.HasError), ElementName=LayoutRoot, Converter={StaticResource NotConverter}}" />
        </WrapPanel>
      </uiLib:BasicMaster.ToolBarArea>
      <uiLib:BasicMaster.MainContent>
        <TabControl x:Name="LayoutRoot">
          <TabControl.BindingGroup>
            <BindingGroup Name="errorGroup" />
          </TabControl.BindingGroup>
          <TabItem Header="{x:Static res:Resources.Main}" Width="Auto">
            <DockPanel>
              <DockPanel>
                <GroupBox Header="{x:Static res:Resources.QueryCondition}" DockPanel.Dock="Top">
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition />
                      <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="100" />
                      <ColumnDefinition Width="160" />
                      <ColumnDefinition Width="100" />
                      <ColumnDefinition Width="160" />
                      <ColumnDefinition Width="100" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Content="{x:Static res:Resources.Dc}" Style="{StaticResource LbStyle}" />
                    <ComboBox Grid.Row="0" Grid.Column="1" Style="{StaticResource ComboStyle}" ItemsSource="{Binding DcCodes,UpdateSourceTrigger=PropertyChanged}" SelectedValue="{Binding SelectDcCode,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Value" DisplayMemberPath="Name" />
                    <Label Grid.Row="0" Grid.Column="2" Content="{x:Static Properties:Resources.PierStatus}" Style="{StaticResource LbStyle}" />
                    <ComboBox Grid.Row="0" Grid.Column="3" Style="{StaticResource ComboStyle}" ItemsSource="{Binding PierStatusList, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Value" SelectedValue="{Binding PierStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Label Grid.Row="1" Grid.Column="0" Content="{x:Static Properties:Resources.DelvDate}" Style="{StaticResource LbStyle}" />
                    <DatePicker Grid.Row="1" Grid.Column="1" SelectedDate="{Binding DelvDate, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DateStyle}" HorizontalAlignment="Left" />
                    <Label Grid.Row="1" Grid.Column="2" Content="{x:Static Properties:Resources.PICK_TIME}" Style="{StaticResource LbStyle}" />
                    <ComboBox Grid.Row="1" Grid.Column="3" Style="{StaticResource ComboStyle}" ItemsSource="{Binding DelvTimeList, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Value" SelectedValue="{Binding SelectDelvTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                  </Grid>
                </GroupBox>
                <GroupBox Header="{x:Static Properties:Resources.DELV_DATE}">
                  <DockPanel>
                    <DataGrid DockPanel.Dock="Left" Style="{StaticResource DgStyle}" SelectionMode="Single" ItemsSource="{Binding DgList,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" SelectedItem="{Binding SelectedData,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                      <DataGrid.Columns>
                        <DataGridTextColumn Header="{x:Static Properties:Resources.DelvDate}" Binding="{Binding DELV_DATE,Converter={StaticResource DateTimeToStringConverter}, ConverterParameter=yyyy/MM/dd}" />
                        <DataGridTextColumn Header="{x:Static Properties:Resources.PICK_TIME}" Binding="{Binding PICK_TIME,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                        <DataGridTextColumn Header="{x:Static Properties:Resources.ALL_NO}" Binding="{Binding ALL_COMP,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                        <DataGridTextColumn Header="{x:Static Properties:Resources.ORD_PIER_CODE}" Binding="{Binding ORD_PIER_CODE,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                        <DataGridTextColumn Header="{x:Static Properties:Resources.TAKE_TIME}" Binding="{Binding TAKE_TIME,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                        <DataGridTextColumn Header="{x:Static Properties:Resources.PIER_CODE}" Binding="{Binding PIER_CODE,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                        <DataGridTextColumn Header="{x:Static Properties:Resources.PierStatus}">
                          <DataGridTextColumn.Binding>
                            <MultiBinding Converter="{StaticResource NameValuePairMultiConverter}">
                              <Binding Path="STATUS" />
                              <Binding Path="DataContext.PierStatusList" RelativeSource="{RelativeSource AncestorType={x:Type uiLib:Wms3plUserControl}}" />
                            </MultiBinding>
                          </DataGridTextColumn.Binding>
                        </DataGridTextColumn>
                      </DataGrid.Columns>
                    </DataGrid>
                    <DockPanel>
                      <GroupBox DockPanel.Dock="Top">
                        <GroupBox.HeaderTemplate>
                          <DataTemplate>
                            <RadioButton Content="{x:Static Properties:Resources.RaPier}" GroupName="x" IsChecked="{Binding DataContext.RaPier, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl},Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                          </DataTemplate>
                        </GroupBox.HeaderTemplate>
                        <StackPanel HorizontalAlignment="Left">
                          <ComboBox Style="{StaticResource ComboStyle}" ItemsSource="{Binding PierList,UpdateSourceTrigger=PropertyChanged}" SelectedValue="{Binding SelectPier,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding RaPier,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" SelectedValuePath="Value" DisplayMemberPath="Name" />
                        </StackPanel>
                      </GroupBox>
                    </DockPanel>
                  </DockPanel>
                </GroupBox>
              </DockPanel>
            </DockPanel>
          </TabItem>
        </TabControl>
      </uiLib:BasicMaster.MainContent>
    </uiLib:BasicMaster>
  </telerik:RadBusyIndicator>
</uiLib:Wms3plUserControl>