<?xml version="1.0" encoding="utf-8"?>
<uiLib:Wms3plWindow x:Class="Wms3pl.WpfClient.P02.Views.P0202030300" xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" FontSize="18" xmlns:uiLib="clr-namespace:Wms3pl.WpfClient.UILib;assembly=Wms3pl.WpfClient.UILib" xmlns:viewModel="clr-namespace:Wms3pl.WpfClient.P02.ViewModel" xmlns:controls="clr-namespace:Wms3pl.WpfClient.UILib.Controls;assembly=Wms3pl.WpfClient.UILib" xmlns:common="clr-namespace:Wms3pl.WpfClient.Common;assembly=Wms3pl.WpfClient.Common" xmlns:Properties="clr-namespace:Wms3pl.WpfClient.P02.Properties" xmlns:res="clr-namespace:Wms3pl.WpfClient.Resources;assembly=Wms3pl.WpfClient.Resources" xmlns:converters="clr-namespace:Wms3pl.WpfClient.Common.Converters;assembly=Wms3pl.WpfClient.Common" controls:PermissionService.FunctionId="P0202030300" controls:PermissionService.FunctionName="檔案上傳" controls:PermissionService.CheckPermission="False" HorizontalAlignment="Stretch" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Width="1024" Height="600" FormException="{Binding Exception}" x:Name="Window">
  <uiLib:Wms3plWindow.DataContext>
    <viewModel:P0202030300_ViewModel x:Name="Vm" />
  </uiLib:Wms3plWindow.DataContext>
  <uiLib:Wms3plWindow.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Wms3pl.WpfClient.UILib;component/Generic.xaml" />
      </ResourceDictionary.MergedDictionaries>
      <common:NotConverter x:Key="NotConverter" />
      <Style TargetType="CheckBox" x:Key="CheckStyle" BasedOn="{StaticResource {x:Type CheckBox}}">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
      </Style>
      <Style TargetType="DataGridCell" x:Key="NumberCellStyle">
        <Setter Property="HorizontalContentAlignment" Value="Right" />
      </Style>
    </ResourceDictionary>
  </uiLib:Wms3plWindow.Resources>
  <telerik:RadBusyIndicator x:Name="GridBusyIndicator" BusyContent="{x:Static res:Resources.Loading}" IsIndeterminate="False" IsBusy="{Binding IsBusy}">
    <uiLib:BasicMaster x:Name="Master">
      <uiLib:BasicMaster.ToolBarArea>
        <WrapPanel>
          <Button Name="btnExit" Style="{StaticResource ExitButton}" Click="btnExit_Click" controls:PermissionService.FunctionId="BP0202030301" />
					<Button Content="{x:Static Properties:Resources.Upload}" controls:PermissionService.FunctionId="BP0202030302" controls:PermissionService.FunctionName="確認上傳" Command="{Binding UploadCommand}" />
        </WrapPanel>
      </uiLib:BasicMaster.ToolBarArea>
      <uiLib:BasicMaster.MainContent>
        <TabControl x:Name="LayoutRoot">
          <TabControl.BindingGroup>
            <BindingGroup Name="errorGroup" />
          </TabControl.BindingGroup>
          <TabItem Header="{x:Static res:Resources.Main}" Width="Auto">
            <DockPanel>
              <!-- 由此處開始加入畫面項目 -->
              <WrapPanel DockPanel.Dock="Top" Margin="5">
                <Label Content="{x:Static Properties:Resources.PURCHASE_NO}" Style="{StaticResource LbStyle}" />
                <Label Content="{Binding PurchaseNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource LbStyle}" />
                <Label Content="{x:Static Properties:Resources.RtNo}" Style="{StaticResource LbStyle}" />
                <Label Content="{Binding RtNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource LbStyle}" />
              </WrapPanel>
              <DataGrid Name="dgList" ItemsSource="{Binding DgList, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DgStyle}" IsReadOnly="True" SelectedIndex="0" VerticalContentAlignment="Center" SelectedItem="{Binding SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <DataGrid.Columns>
                  <DataGridTemplateColumn Header="{x:Static Properties:Resources.IsSelectedUpload}" IsReadOnly="False">
                    <DataGridTemplateColumn.CellTemplate>
                      <DataTemplate>
                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CheckStyle}" />
                      </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                  </DataGridTemplateColumn>
                  <DataGridTextColumn Header="{x:Static Properties:Resources.UPLOAD_NAME}" Binding="{Binding Item.UPLOAD_NAME}" />
                  <DataGridTextColumn Header="{x:Static Properties:Resources.SELECTED_COUNT}" Binding="{Binding Item.SELECTED_COUNT}" />
                  <DataGridTextColumn Header="{x:Static Properties:Resources.UPLOAD_DESC}" Binding="{Binding Item.UPLOAD_DESC}" />
                  <DataGridTemplateColumn Header="{x:Static Properties:Resources.View}">
                    <DataGridTemplateColumn.CellTemplate>
                      <DataTemplate>
												<Button Name="btnChooseFile" Content="{x:Static Properties:Resources.ChooseFile}" Click="btnChooseFile_Click" controls:PermissionService.FunctionId="BP0202030303" controls:PermissionService.FunctionName="選擇檔案" />
                      </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                  </DataGridTemplateColumn>
                  <DataGridTemplateColumn Header="{x:Static Properties:Resources.Clear}">
                    <DataGridTemplateColumn.CellTemplate>
                      <DataTemplate>
												<Button Name="btnClearFile" Content="{x:Static Properties:Resources.ClearFile}" Click="btnClearFile_Click" controls:PermissionService.FunctionId="BP0202030304" controls:PermissionService.FunctionName="清空檔案" />
                      </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                  </DataGridTemplateColumn>
                  <DataGridTextColumn Header="{x:Static Properties:Resources.UPLOADED_COUNT}" Binding="{Binding Item.UPLOADED_COUNT}" />
                </DataGrid.Columns>
              </DataGrid>
            </DockPanel>
          </TabItem>
        </TabControl>
      </uiLib:BasicMaster.MainContent>
    </uiLib:BasicMaster>
  </telerik:RadBusyIndicator>
</uiLib:Wms3plWindow>